# Backend API URL (with or without protocol - http:// will be added if missing)
NEXT_PUBLIC_API_URL=localhost:4000

# WebSocket URL (without protocol - ws:// or wss:// will be added automatically)
NEXT_PUBLIC_WS_URL=localhost:4000